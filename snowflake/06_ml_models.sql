-- ============================================================================
-- 06_ML_MODELS.SQL — ML Functions for Fabric Supply Chain
-- ============================================================================
USE DATABASE TEXTILE_SUPPLY_CHAIN;
USE SCHEMA ML;

-- ML.FORECAST: TEXTILE_SUPPLY_CHAIN_FORECAST
CREATE OR REPLACE SNOWFLAKE.ML.FORECAST ML.TEXTILE_SUPPLY_CHAIN_FORECAST(
  INPUT_DATA => SYSTEM$REFERENCE('TABLE', 'CURATED.FORECAST_INPUT'),
  SERIES_COLNAME => 'ENTITY_ID',
  TIMESTAMP_COLNAME => 'DS',
  TARGET_COLNAME => 'Y'
  
);

CREATE OR REPLACE TABLE ML.TEXTILE_SUPPLY_CHAIN_FORECAST_RESULTS AS
SELECT * FROM TABLE(ML.TEXTILE_SUPPLY_CHAIN_FORECAST!FORECAST(FORECASTING_PERIODS => 14));

-- ML.ANOMALY_DETECTION: TEXTILE_SUPPLY_CHAIN_ANOMALY
CREATE OR REPLACE SNOWFLAKE.ML.ANOMALY_DETECTION ML.TEXTILE_SUPPLY_CHAIN_ANOMALY(
  INPUT_DATA => SYSTEM$REFERENCE('TABLE', 'CURATED.TREND_ANALYTICS'),
  SERIES_COLNAME => 'ENTITY_ID',
  TIMESTAMP_COLNAME => 'DS',
  TARGET_COLNAME => 'Y',
  LABEL_COLNAME => ''
);

CREATE OR REPLACE TABLE ML.TEXTILE_SUPPLY_CHAIN_ANOMALY_RESULTS AS
SELECT * FROM TABLE(ML.TEXTILE_SUPPLY_CHAIN_ANOMALY!DETECT_ANOMALIES(INPUT_DATA => SYSTEM$REFERENCE('TABLE', 'CURATED.TREND_ANALYTICS'), SERIES_COLNAME => 'ENTITY_ID', TIMESTAMP_COLNAME => 'DS', TARGET_COLNAME => 'Y'));

